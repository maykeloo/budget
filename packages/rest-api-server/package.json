{
  "name": "@actual-app/rest-api-server",
  "version": "1.0.0",
  "license": "MIT",
  "description": "HTTP REST API server for Actual Budget",
  "engines": {
    "node": ">=20"
  },
  "main": "dist/server.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsc && node dist/server.js",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@actual-app/api": "workspace:^",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "helmet": "^8.0.0"
  },
  "devDependencies": {
    "@types/compression": "^1.7.5",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.0",
    "@types/node": "^22.17.0",
    "typescript": "^5.9.2"
  }
}
